using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeroElementHolder : MonoBehaviour
{
    public bool neutral;
    public bool earth;
    public bool fire;
    public bool water;
    public bool lightning;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
