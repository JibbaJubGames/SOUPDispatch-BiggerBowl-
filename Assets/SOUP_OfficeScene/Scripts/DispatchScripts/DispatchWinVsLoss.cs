using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DispatchWinVsLoss : MonoBehaviour
{
    public static int dispatchesWon;
    public static int dispatchesLost;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
