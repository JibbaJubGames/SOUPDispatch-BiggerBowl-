using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PrankCallAddressCheck : MonoBehaviour
{
    public bool prankCaller = false;
    //Have a dictionary in the game manager hold a key value pair of address int and bool value;
    //if the address number is on the dictionary, it's a prank call address
    public int overallAddressNumber;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
